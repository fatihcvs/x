node_modules/
.env
data.json*
data.db*
*.log

# local packaging artifacts
ai-twitter-copilot.zip
_tmp_extract/

# unrelated nested project (its own git repo — must not be tracked here)
publishbot-inspect/
